server:
  port: 8080

spring:
  application:
    name: navops-api
  
  datasource:
    url: jdbc:postgresql://localhost:5433/navops
    username: navops_user
    password: 2102
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: "navops.soporte@gmail.com"
    password: "pffipftmhsofjywe"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Configuraciones base de CORS para la PWA
cors:
  allowed-origins: http://localhost:5173

# Documentación de la API (Swagger)
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html


